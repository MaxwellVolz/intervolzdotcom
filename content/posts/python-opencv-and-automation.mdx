---
title: Python - OpenCV and Automation
date: 2025-06-21T13:14:00.000Z
---

## Scoping

### Goal

Easy to setup generic automation that obliterates AutoHotKey

### Phases of Development

1. Python project setup for Windows
   1. Readme with Usage and Install
2. Press Keyboard/Mouse Button on timed loop
3. Moving the Mouse
4. Taking screenshot of specific window
5. Processing screenshot and triggering action

### Architecture

1. Python with `venv`
2. Input Control dependencies: `pyautogui` `pydirectinput`, `pynput.mouse`

## Implementation

### Python

### Testing Input Controls
input_controller.py
```py
import time
import pyautogui
import pydirectinput
from pynput.mouse import Controller, Button

def right_click_pyautogui():
    print("Performing right click using pyautogui...")
    pyautogui.click(button="right")

def right_click_pynput():
    print("Performing right click using pynput...")
    mouse = Controller()
    mouse.click(Button.right, 1)

def press_key_pydirectinput():
    print("Pressing 'K' key using pydirectinput...")
    pydirectinput.press("k")
```

### Backlog

1. OpenCV